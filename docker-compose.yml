services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gramconnect
      POSTGRES_USER: gramconnect
      POSTGRES_PASSWORD: gramconnect
    ports:
      - "5432:5432"
    volumes:
      - gramconnect-data:/var/lib/postgresql/data
volumes:
  gramconnect-data:
